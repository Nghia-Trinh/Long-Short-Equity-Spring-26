baseURL = "/"
languageCode = "en-us"
title = "Model & FinnHub Results"

# Include tracked chart artifacts from previous agent outputs.
staticDir = ["static", "outputs/charts"]

# Keep this deployment as a single-page site without taxonomies.
disableKinds = ["taxonomy", "term", "rss"]
